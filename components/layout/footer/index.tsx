"use client";

export default function Footer() {
  return (
    <footer className="border-t border-[var(--color-border)] px-6 py-3">
      <p className="text-[10px] text-[var(--color-text-muted)] font-[var(--font-mono)] text-center">
        FlowTrack — Project Tracking for Small Teams
      </p>
    </footer>
  );
}
